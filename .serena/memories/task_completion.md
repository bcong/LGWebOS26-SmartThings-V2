# Task Completion
- For packaging validation, use the documented SmartThings CLI package command from the repository root.
- For runtime diagnosis, use SmartThings CLI `edge:drivers:logcat`; live TV behavior and pairing require an actual SmartThings Hub and LG webOS TV.
- No local Lua test suite or formatter is documented, so report static checks separately from deployment/runtime checks.